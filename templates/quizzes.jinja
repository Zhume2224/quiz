{% extends 'base.jinja' %}

{% block content %}
<h1>Users</h1>
<p>section1: this area shows the current users. this area allows the user to choose their names</p>
<h1>Quizzes</h1>
<p>section2: this area shows the quizzes selected fo the current user</p>




{% for quiz in quizzes%}
<form action="/quizzes" method="post" >
{# user select here #}
<input type="hidden" name="quiz_id" value="{{quiz.id}}"/>
<label>Question: {{quiz.quiz}}</label>
<select name="option">
  <option name="opt1" value="{{quiz.opt1}}">{{quiz.opt1}}
  </option><br>
  <option name="opt2" value="{{quiz.opt1}}">{{quiz.opt2}}
  </option><br>
  <option name="opt3" value="{{quiz.opt1}}">{{quiz.opt3}}
  </option><br>
</select>
 <input type="submit" value="Submit"><br>
 </form>
{% endfor %}


{% endblock %}

