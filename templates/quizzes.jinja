{% extends 'base.html' %}

{% block content %}
<h1>Users</h1>
<p>section1: this area shows the current users. this area allows the user to choose their names</p>
<h1>Quizzes</h1>
<p>section2: this area shows the quizzes selected fo the current user</p>

{% endblock %}


{# 
<p >
  <a class="btn btn--action" href="/visits/new">New Users</a>
</p> #}
{# 
<ul>
  {% for visit in visits %}
  <li>
  <p>{{ visit.user.name }} - {{ visit.location.name }} </p>
  <p>"<i>{{visit.review}}</i>"</p>
    <p>
      <form action="/visits/{{ visit.id }}/delete" method="POST">
        <input class="btn btn--danger" type="submit" value="delete" />
      </form>
    </p>
  </li>
  {% endfor %}
</ul> #}

